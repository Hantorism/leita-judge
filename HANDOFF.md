# 인계 문서 (HANDOFF.md)

이 문서는 이전 작업 세션의 진행 경과, 핵심 설계 결정과 그 근거, 실측 수치, 운영 환경의 특이사항, 향후 과제를 다음 작업자에게 전달하기 위해 작성되었습니다. 코드베이스 아키텍처와 기본 실행 명령어에 관한 내용은 [CLAUDE.md](file:///Users/zzang12/Workspace/Leita/judge/CLAUDE.md)에 상세히 기록되어 있으므로 해당 문서를 참조하십시오.

---

## 1. 현재 상태 요약

cgroup v2 기반 메모리 측정 계층 신설부터 언어별 시간 및 메모리 버퍼 적용, 신규 지원 언어 4종 추가까지 모든 작업이 dev 브랜치에 병합되었으며 dev 클러스터에서 실측 검증까지 완료된 상태입니다. 현재 지원 언어는 C, C++, Java, Python, JavaScript, Go, Kotlin, Swift, Rust, C#, TypeScript로 총 11종입니다.

---

## 2. 완료된 작업 이력

- **cgroup v2 기반 메모리 측정 계층 신설**: `src/cgroup` 패키지를 추가하고 executor 계층과 통합하여 dev 클러스터 환경에서 실측 검증을 완료했습니다.
- **MEMORY_OUT 정식 판정 및 메모리 버퍼 적용**: 메모리 초과 상황을 판별하는 정식 체계를 마련하고 언어별 특성에 맞춘 메모리 버퍼를 적용했습니다.
- **언어별 시간 버퍼 도입**: 백준 온라인 저지(BOJ) 정책을 참고하여 언어별 시간 배수 및 가산 버퍼를 도입했습니다.
- **신규 언어 지원 및 기존 언어 재활성화**: Swift를 재활성화하고 Rust, C#, TypeScript를 신규 언어로 추가했습니다. 각 언어별 정상 채점, 컴파일 에러 판정, 채점 완료 후 빌드 산출물 삭제를 dev 클러스터에서 직접 확인했습니다.
- **JVM 실행 커맨드 최적화**: JVM 실행 옵션에서 `-Xms` 설정을 제거했습니다.
- **미지원 언어 방어 로직 추가**: `language.Supports` 함수를 도입하여 지원 목록에 없는 언어 요청이 들어왔을 때 발생하는 런타임 인덱스 패닉을 차단했습니다.
- **GitOps 매니페스트 갱신**: GitOps 저장소([/Users/zzang12/Workspace/Leita/manifest](file:///Users/zzang12/Workspace/Leita/manifest))의 main 브랜치에 신규 언어 3종의 Deployment, Service, Ingress 경로와 Tekton 파이프라인 배열을 반영하고 Swift 빌드 스킵 조건을 제거했습니다.

---

## 3. 재논의 방지를 위한 설계 결정과 근거

동일한 논의가 불필요하게 반복되지 않도록 주요 의사결정 사항과 그 배경을 정리합니다.

### 3.1. 측정 범위 한정 (실행 단계만 측정)
- **결정**: cgroup을 통한 자원 측정 범위는 소스 코드 실행 단계(표준 입력 읽기부터 표준 출력 쓰기까지)로 한정하며, 컴파일 및 빌드 산출물 삭제 단계는 측정에서 제외합니다.
- **근거**: 컴파일러나 런타임 빌드 도구가 소모하는 메모리가 사용자의 채점 결과에 포함되면 순수한 알고리즘 메모리 사용량을 왜곡하게 됩니다.

### 3.2. 판정 우선순위 (OOM → 타임아웃 → 런타임 에러)
- **결정**: 실행 결과 판정 우선순위는 `JudgeMemoryOut`(OOM)을 가장 먼저 확인하고, 그 다음 `JudgeTimeOut`, 마지막으로 `JudgeRuntimeError`를 검사합니다.
- **근거**: 리눅스 커널의 OOM Killer는 프로세스에 `SIGKILL` 신호를 전송합니다. 따라서 우선순위를 변경하면 메모리 초과로 강제 종료된 프로세스가 일반 비정상 종료(런타임 에러)나 타임아웃으로 잘못 분류됩니다.

### 3.3. 시간 버퍼와 메모리 버퍼의 산정 방식 분리
- **결정**: 시간 버퍼는 배수와 가산을 함께 적용하고, 메모리 버퍼는 고정 가산 방식만을 적용합니다.
- **근거**: 인터프리터 언어의 실행 지연은 코드의 작업량에 비례하여 증가하므로 배수 보정이 필수적입니다. 반면 런타임 초기화와 가비지 컬렉터(GC)에 필요한 메모리 오버헤드는 문제의 제한 수치와 무관한 고정 상수이기 때문에 고정 가산이 적합합니다.

### 3.4. C 및 C++ 메모리 버퍼 미부여
- **결정**: C와 C++ 언어에는 메모리 버퍼를 일절 부여하지 않습니다.
- **근거**: 알고리즘 문제의 메모리 제한 기준 자체가 C/C++을 기반으로 설계됩니다. 여기에 임의의 마진을 부여하면 16MB 제한과 같은 메모리 최적화 문제의 출제 의도가 무력화됩니다. 실측 결과에서도 12MB를 소모하는 풀이가 13MB 제한에서는 정상 통과하고 12MB 제한에서는 정확히 OOM 처리되어, 순수 오버헤드가 0.3MB에 불과함을 확인했습니다.

### 3.5. JVM 실행 옵션의 -Xms 제거 및 -Xmx 유지
- **결정**: JVM 실행 커맨드에서 `-Xms` 옵션을 완전히 제거하고, `-Xmx` 옵션은 문제 제한보다 충분히 큰 상태로 유지합니다.
- **근거**: 초기 힙 크기(`-Xms`)를 크게 지정하면 JVM이 힙 메모리가 여유롭다고 판단하여 가비지 컬렉션을 미루게 되고, 결국 cgroup 상한에 먼저 도달하여 정상 코드가 `MEMORY_OUT`으로 오판정됩니다. 실제 동일 부하 테스트에서 `-Xms1024m` 설정 시 224MB를 점유하던 코드가 옵션 제거 후 96MB로 감소했습니다. 반대로 `-Xmx`를 문제 제한 수준으로 낮추면 JVM 내부에서 `OutOfMemoryError`가 발생하여 cgroup OOM 대신 `RUNTIME_ERROR`로 결과가 왜곡되므로, cgroup 레벨에서 제어되도록 `-Xmx`를 크게 열어두어야 합니다.

### 3.6. Rust edition 2021 채택
- **결정**: Rust 컴파일 옵션으로 `edition 2021`을 고정 적용합니다.
- **근거**: `edition 2024`에서는 `static mut` 전역 변수에 대한 참조 생성이 컴파일 하드 에러로 처리됩니다. 이는 경쟁 프로그래밍 풀이에서 자주 활용되는 전역 배열 접근 패턴을 손상시킵니다. 추후 버전 전환이 필요할 때에는 컴파일 플래그 수정만으로 가능하며 버퍼 수치에는 영향을 주지 않습니다.

### 3.7. bash 지원 제외 결정 (2026년 9월 11일)
- **결정**: bash를 채점 지원 언어로 채택하지 않기로 최종 확정했습니다.
- **근거**: 실측 결과 bash의 단순 산술 반복 연산 속도는 C 언어 대비 약 550배 느립니다(반복당 C 언어 0.0042μs 대비 bash 2.3μs 소모). 일반적인 문제에서 bash 코드를 통과시키려면 시간 버퍼를 수십 배 이상 부여해야 하는데, 이는 타 언어와의 형평성 원칙을 심각하게 훼손합니다. 현실적인 시간 버퍼를 부여할 경우에는 대다수 문제에서 무조건 `TIME_OUT`이 발생합니다. 이는 bash 인터프리터가 바이트코드 컴파일 과정 없이 매 실행마다 명령 트리를 재해석하고, 구문마다 8단계의 단어 확장을 수행하며, 모든 변수를 해시 테이블 내 문자열로 관리하는 태생적 구조에 기인합니다.

---

## 4. 실측 데이터

### 4.1. 언어별 런타임 오버헤드 및 최소 동작 하한선
30MB를 일정하게 점유하는 로직을 수행할 때 소모되는 순수 오버헤드와, 가장 단순한 최소 프로그램(A+B 덧셈) 실행 시 요구되는 런타임 메모리 하한선입니다.

| 언어 | 순수 메모리 오버헤드 (30MB 기준) | 런타임 메모리 하한선 | 비고 |
|---|---|---|---|
| C / C++ | 0.3 MB | 2.0 MB 미만 | 12MB 워크로드가 13MB 제한 통과, 12MB 제한에서 OOM 확인 |
| Rust | 1.0 MB | 2MB 이하 | edition 2021 적용 |
| Swift | 1.8 MB | 4MB 이하 (그 미만은 미측정) | |
| Python | 3.9 MB | 5.0 MB 이하 | |
| C# (.NET) | 5.7 MB | 16MB 이하 (그 미만은 미측정) | 런타임 초기 구동 비용 포함 |
| Go | 6.1 MB | 4.0 MB 이하 | 런타임 고루틴 및 GC 스케줄러 기본 비용 |
| JavaScript (Node.js) | 9.2 MB | 12.0 MB | 8MB 제한 환경에서는 즉시 OOM 발생 |
| TypeScript (Node.js) | 9.4 MB | 12.0 MB | 8MB 제한 환경에서는 즉시 OOM 발생 |
| Java / Kotlin (JVM) | 39.2 MB | 32.0 MB | 24MB 제한 환경에서는 즉시 OOM 발생 |

### 4.2. 언어별 상대 실행 속도 비교
동일한 반복 워크로드를 기준으로 C 언어의 실행 소요 시간을 1.0배로 두었을 때의 상대 속도입니다.

| 언어 | C 언어 대비 상대 수행 시간 | 비고 |
|---|---|---|
| C | 1.0배 | 기준치 (0.0042 μs / op) |
| Rust | 1.0배 | C 언어와 동등한 네이티브 성능 |
| Swift | 1.0배 | 네이티브 컴파일 성능 |
| TypeScript | 1.4배 | 동일한 Node.js 런타임 환경 (JS와의 차이는 측정 노이즈이며 메모리 특성 동일) |
| C# | 1.9배 | CoreCLR JIT 구동 |
| JavaScript | 2.4배 | 동일한 Node.js 런타임 환경 (TS와의 차이는 측정 노이즈이며 메모리 특성 동일) |
| Python | 24.0배 | CPython 인터프리터 오버헤드 |
| bash | 약 550.0배 | 지원 제외 판정의 핵심 근거 (2.3 μs / op) |

---

## 5. 운영 환경 정보

- **클러스터 사양**: Oracle Cloud Infrastructure(OCI) OKE `leita-cluster` (Kubernetes v1.36.1), 리전은 `ap-chuncheon-1`입니다.
- **노드 사양**: Oracle Linux 9.7, 커널 버전 6.12 (UEK8), 컨테이너 런타임 CRI-O 1.36, 아키텍처 arm64입니다. cgroup v2를 전면 사용합니다.
- **노드풀 식별자**: `ocid1.nodepool.oc1.ap-chuncheon-1.aaaaaaaazyngkob66r6so4ap36yeebe7fszum7m56ns2vnm46nzxqxkprixa`
- **부트 볼륨 성능**: 용량은 100GB이며 성능은 120 VPU로 설정되어 있습니다. 노드풀 생성 API에는 VPU 설정 필드가 존재하지 않으므로, 노드 교체나 풀 재생성 시 아래 OCI CLI 명령을 수동으로 수행해야 합니다:
  ```bash
  oci bv boot-volume update --vpus-per-gb 120 --boot-volume-id <BOOT_VOLUME_OCID>
  ```
- **judge 파드 권한 요구사항**: cgroup 조작을 위해 `securityContext.privileged: true` 설정이 필수적입니다. 권한이 없을 경우 서버 자체는 정상 기동되지만 `usedMemory`가 항상 0으로 집계되는 폴백 모드로 동작합니다.
- **GitOps 배포 저장소**: 매니페스트는 [/Users/zzang12/Workspace/Leita/manifest](file:///Users/zzang12/Workspace/Leita/manifest) 저장소의 main 브랜치에 직접 커밋 및 푸시하여 반영합니다. ArgoCD가 `judge/dev` 및 `judge/prod` 디렉터리의 변경을 감시하여 배포합니다.
- **Redis 종속성**: 서버는 기동 시점에 Redis 연결을 확인하며, 연결 실패 시 즉시 프로세스가 중단(`Fatal`)됩니다. 로컬 환경 실행 및 E2E 테스트 진행 시에도 반드시 Redis 컨테이너가 동작 중이어야 합니다.

---

## 6. 향후 과제 및 작업 목록

| 우선순위 | 작업 항목 | 세부 내용 및 고려사항 |
|---|---|---|
| 높음 | judge Dockerfile 이미지 최적화 | Swift 이미지(3.17GB)에서 `sourcekit-lsp`(188MB), `LLDB`(약 300MB), `libsourcekitdInProc`(177MB), `swift-package`(157MB), `swift_static`(167MB)을 제거하고 `FROM scratch`로 레이어를 평탄화하면 2.08GB로 감소함을 이미 검증했습니다. 단순히 `RUN rm -rf`만 수행하면 이전 레이어가 잔존하여 용량이 줄지 않으므로 다단계 빌드를 활용해야 합니다. 공식 slim 태그는 컴파일러가 없어 사용 불가합니다. Rust(1.11GB)와 C#(942MB) 이미지에도 동일한 최적화 기법을 적용할 수 있습니다. |
| 높음 | 채점 환경 격리 강화 검토 | 현재 사용자 제출 코드가 root 권한 및 privileged 컨테이너 내부에서 실행됩니다. 악의적인 코드가 파드에 마운트된 OCI 서비스 자격 증명을 탈취하거나 타 파드의 cgroup 및 호스트 자원을 조작할 수 있으므로 샌드박스 격리 방안을 검토해야 합니다. |
| 중간 | CI 테스트 파이프라인 누락 점검 | 이전 Redis 도입 커밋에서 `service_test.go` 파일이 컴파일 불가능한 상태로 dev 브랜치에 병합되었으나 CI 단계에서 차단되지 않았습니다. CI 워크플로가 유닛 테스트를 정상적으로 구동하고 있는지 확인해야 합니다. |
| 중간 | 프로덕션(prod) 환경 배포 | 현재 변경 사항은 dev 클러스터에서만 검증을 마쳤습니다. prod 환경용 매니페스트는 준비되어 있으므로 운영 배포 절차를 진행해야 합니다. |
| 낮음 | C/C++ 메모리 마진 제거 공지 검토 | C/C++ 언어에 버퍼를 부여하지 않게 됨에 따라 기존에 마진으로 인해 통과하던 경계선 풀이가 `MEMORY_OUT`으로 판정될 수 있습니다. 사용자 대상 정책 공지 여부를 검토해야 합니다. |
| 낮음 | 원격 저장소 잔여 브랜치 정리 | 원격 저장소에 남아 있는 기존 작업 브랜치(`check`, `judge-mq`, `refactor/architecture`, `fix/dataSource`)를 정리해야 합니다. |

---

## 7. 작업 방식 및 개발 선호사항

- **커밋 시점 통제**: 사용자가 명시적으로 지시한 시점에만 커밋을 수행합니다. 작업을 완료했다고 해서 임의로 커밋하거나 원격 저장소로 푸시하지 않습니다.
- **실행 환경 분리**: macOS 로컬에서는 코드 작성과 정적 분석만 진행하며, 서버 실행과 모든 테스트는 Docker 컨테이너 내부에서 실행합니다. cgroup 연동 테스트를 돌릴 때는 `--privileged` 플래그가 필수입니다.
- **논리적 커밋 분리**: 여러 변경 사항을 한꺼번에 묶지 않고 논리적인 작업 단위별로 분리하여 커밋합니다.
- **커밋 메시지 규칙**: 커밋 메시지는 영어로 작성하며, [CLAUDE.md](file:///Users/zzang12/Workspace/Leita/judge/CLAUDE.md)의 Commit Message Convention(유형 접두사, 한 줄 요약, 본문 서술 구조)을 준수합니다.

---

## 8. 참고 문서

- **상세 설계 계획서**: `/Users/zzang12/Downloads/cgroup-memory-plan.md` 및 동명의 `.html` 파일에 Phase 1부터 7까지의 설계, 실측 데이터, 검증 과정이 상세하게 기록되어 있습니다.
- **프로젝트 기본 규칙 및 아키텍처**: [CLAUDE.md](file:///Users/zzang12/Workspace/Leita/judge/CLAUDE.md)
